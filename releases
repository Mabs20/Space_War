import sys
import os

if getattr(sys, 'frozen', False):
    current_path = sys._MEIPASS
else:
    current_path = os.path.dirname(__file__)

ship_img = pygame.image.load(current_path  + '/assets/images/playerShip1_red.png')
